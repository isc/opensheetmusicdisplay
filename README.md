# PR assets

Permanent branch hosting screenshots referenced from pull request descriptions
(GitHub's user-attachments upload has no API, so images are committed here and
referenced via raw.githubusercontent.com URLs). One directory per PR.

Not meant to be merged anywhere — do not open a pull request from this branch,
and do not delete it: the PR descriptions hotlink these files.
